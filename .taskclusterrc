---
  provisionerId: "aws-provisioner-v1"
  workerType: "b2gtest"
  payload:
    image: "ubuntu:latest"
    command:
      - "/bin/bash"
      - "-c"
      - "git clone ${GITHUB_HEAD_REPO_URL} && cd releasetasks && git checkout ${GITHUB_HEAD_SHA} && tox -e py27u"
    maxRunTime: 3600
  metadata:
    name: "Redo - Utilities to retry Python callables"
    description: "Redo - Utilities to retry Python callables"
    owner: "bhearsum@mozilla.com"
    source: "https://github.com/bhearsum/redo"
